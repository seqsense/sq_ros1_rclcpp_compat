# sq_ros1_rclcpp_compat
ROS 2 rclcpp API shim for ROS 1, plus rclcpp-style headers for common message packages. Lets the same source build on ROS 1 and ROS 2 (no-op on ROS 2). Includes sample hybrid packages.
