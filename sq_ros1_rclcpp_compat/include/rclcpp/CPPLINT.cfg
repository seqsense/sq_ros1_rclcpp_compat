# These shim headers intentionally use non-standard guard names (e.g.
# ROS1_COMPAT__RCLCPP__CLOCK_HPP_) to avoid conflicting with the real
# rclcpp headers when both include paths coexist in a ROS 2 build.
filter=-build/header_guard
